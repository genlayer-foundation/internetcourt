[pytest]
# Exclude integration tests by default (they need a running GenLayer network)
# Run integration tests explicitly with: gltest -m integration contracts/tests/integration/
addopts = -m "not integration"

markers =
    integration: marks tests that deploy to a real GenLayer network (deselect with '-m "not integration"')
